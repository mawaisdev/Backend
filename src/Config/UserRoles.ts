export enum UserRole {
  Admin = 'Admin',
  User = 'User',
}

/*
TODO: Roles, Permissions

Admin, User

helper folder should be on main directory.
Need to update the string with constants in catch block.
dont user direct function response in if conditions.
create the separate file for checking the env and set the config on the basis of env. dont use hard coded values in config.
write migeration for entities and dont create or update direct from model or entity.

Permission: create, read, update, delete
Create Migrations
*/
